# 🗄️ SQL — Estudos e Aplicações Práticas

Esta pasta reúne estudos estruturados de **SQL (Structured Query Language)** aplicados a diferentes contextos e ambientes.

O objetivo é consolidar fundamentos da linguagem, explorar boas práticas de consulta e organização de dados, e compreender como o SQL se integra a pipelines analíticos e arquiteturas modernas de dados.

Cada subpasta representa um estudo ou projeto específico, organizado por ambiente ou abordagem técnica.

## 🎯 Objetivo da Seção

- Dominar fundamentos da linguagem SQL;
- Aplicar consultas em diferentes ambientes;
- Estruturar dados de forma relacional;
- Organizar transformações em pipelines;
- Consolidar base para engenharia e análise de dados.

## 🧠 Metodologia

Para cada biblioteca estudada:

1. Reprodução de exemplos básicos;
2. Desenvolvimento de pequenos scripts práticos;
3. Testes exploratórios;
4. Registro de aprendizados com base em projetos reais no README específico.

## 📚 Assuntos Explorados
Cada pasta possui sua própria descrição detalhada e exemplos práticos aplicados em contexto real ou simulado.

📁 **`1. Fundmentos Básicos com Databricks`**

- Introdução à linguagem SQL em ambiente distribuído;
- Manipulação e consulta de dados com `SELECT`, `WHERE`, `GROUP BY`, `ORDER BY`;
- Uso de funções agregadas (`COUNT`, `SUM`, `AVG`, etc.);
- Conversão e tratamento de tipos com `CAST`;
- Utilização de CTEs (`WITH`) para organização de queries;
- Criação de tabelas com `CREATE TABLE` e `CTAS`;
- Aplicação da arquitetura **Medallion (Bronze, Silver, Gold)** para organização em camadas;
- Estruturação de pipeline analítico dentro do Databricks.

📁 **`2. `**

- 
